# Architecture Decision Records

One file per decision, named `NNNN-short-title.md` (e.g. `0001-use-kafka-for-order-events.md`). Copy `0000-template.md` to start a new one.

Record a decision here when it affects the architecture map, a data store choice, a Kafka topic/schema convention, or anything hard to reverse — not for routine implementation choices.
