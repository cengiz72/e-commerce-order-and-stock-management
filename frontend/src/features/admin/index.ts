/**
 * `features/admin` — inventory admin panel (docs/frontend-architecture.md §3.4).
 *
 * Placeholder page only so far; catalog/stock writes land in Phase 2.
 */
export { AdminInventoryPage } from './AdminInventoryPage'
