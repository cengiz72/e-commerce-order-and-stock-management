/**
 * `features/orders` — order tracking (docs/frontend-architecture.md §3.3).
 *
 * Placeholder page only so far; `orderService` reads and refetch timing land in Phase 2.
 */
export { OrderTrackingPage } from './OrderTrackingPage'
