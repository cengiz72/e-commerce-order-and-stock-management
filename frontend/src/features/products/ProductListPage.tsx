/**
 * Placeholder for the storefront product list & search (docs/frontend-architecture.md §3.1).
 *
 * Routing skeleton only (SCRUM-4): no product data, no `productService` call. Phase 2 fills
 * this in — the route, and the fact that it is public, are what exist today.
 */
export function ProductListPage() {
  return (
    <section>
      <h1>Products</h1>
      <p>Product listing and search arrive in Phase 2.</p>
    </section>
  )
}
