/**
 * `features/products` — product listing/search (docs/frontend-architecture.md §3.1).
 *
 * Placeholder pages only so far; the data hooks and `productService` calls land in Phase 2.
 */
export { ProductDetailPage } from './ProductDetailPage'
export { ProductListPage } from './ProductListPage'
