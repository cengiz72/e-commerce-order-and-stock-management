/**
 * `features/cart` — cart and checkout (docs/frontend-architecture.md §3.2).
 *
 * Placeholder pages only so far; `cartService`/`orderService` calls land in Phase 2.
 */
export { CartPage } from './CartPage'
export { CheckoutPage } from './CheckoutPage'
