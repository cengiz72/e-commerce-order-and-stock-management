# Base URLs used by the shared HTTP client (src/services/http).
# Copy to .env.local and adjust once the backend services actually expose ports.
# The values below are placeholders: no app service ports are defined in docker-compose.yml yet.

VITE_PRODUCT_SERVICE_URL=http://localhost:8081
VITE_ORDER_SERVICE_URL=http://localhost:8082
VITE_PAYMENT_SERVICE_URL=http://localhost:8083
VITE_USER_SERVICE_URL=http://localhost:8084
